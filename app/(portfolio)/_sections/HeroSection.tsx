"use client";

import gsap from "gsap";
import { useGSAP } from "@gsap/react";

const HeroSection = () => {
  return (
    <section className="relative w-full h-dvh">
      <div></div>

      <div className="absolute bottom-0 left-0 flex items-center justify-between"></div>
    </section>
  );
};

export default HeroSection;
